.DS_Store
/.build
/Packages
xcuserdata/
DerivedData/
.swiftpm/xcode/package.xcworkspace/contents.xcworkspacedata
.swiftpm/xcode/xcshareddata/
*.xcodeproj
# macOS build bundle at repo root only — anchored so it doesn't match the Windows
# `Switcher3way.App` project dir (git is case-insensitive on Windows: .App == .app).
/Switcher3way.app
*.dmg
*.zip
.vscode/
.claude/
*.certSigningRequest

# self-signed code-signing key — never commit
signing/cert.p12
signing/key.pem
signing/*.pfx
